如 closure所示：
将一个函数写在另一个函数之内，那么这个位于内，那么这个位于内部的函数便可以访问
外部函数中的局部变量，这个特性成为词法域。
（词法域是一种用来定义在Scheme，CommonLisp，Pascal，Algol中环境及外部变量的方法，词法域不仅需要跟踪程序的语法顺序，
并且也要遵循程序运行时的执行顺序来完成。这表明Scheme的过程调用比C或者Fortran这类面向对象程序要求更加多的资源需求。）








